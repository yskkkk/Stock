# 폴링 — 다시 기동 버튼

중지된 폴러 카드·상세 패널에 **다시 기동** 버튼을 분리해 추가했습니다.

- 실행 중: **중지**만 표시
- 중지됨: **다시 기동**(primary)만 표시
- 상세 패널 하단에도 동일 버튼
- 관리자 비밀번호 확인 후 `runtimeEnabled=true`로 복구

새로고침 후 폴링 패널에서 중지 → 다시 기동 흐름 확인.
