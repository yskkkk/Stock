# [보고서] AI 트레이딩 모델 매수/매도 시나리오 분석

## 1. 개요

* **분석 대상:** YSTOCK 서버 기반 **기술적 분석 스크리너 + 실매매/시뮬 프로그램** 파이프라인 (`server/screener.js`, `server/technical.js`, `server/live-trade-runner.js`, `server/live-trade-auto-sell.js`, `server/live-trade-sell-strategy.js`, `server/live-trade-exit-scenario.js`, `server/bithumb-trading-adapter.js`, `server/toss-trading-adapter.js`)
* **주요 요약:** 전 종목을 일봉 기준 **12가지 기술 신호**로 걸러낸 뒤, 프로그램별 **최소 점수(가중치 비율)**·시장·보유 한도를 통과한 종목만 **정액 매수**하고, 매수 시 산출한 **목표가·손절가** 및 **단기/중기/장기 매도 전략**으로 자동 청산(또는 시뮬 기록)합니다.

---

## 2. 매수(Buy) 시나리오 및 조건

시스템의 매수는 크게 **① 스크리너 후보 선정 → ② 고득점 알림/실행 트리거 → ③ 프로그램별 게이트 → ④ 주문·기록** 순으로 이어집니다.

### 🔹 시나리오 A: 스크리너 기술적 분석 통과 (1차 후보)

* **설명:** 주기적 스캔에서 종목·시장(국내/미국/코인)별로 차트를 읽고, 활성화된 **기술 분석 모델**마다 매수 후보인지 판별합니다.
* **핵심 지표/변수:** `candles`(일봉 55봉 이상), `SIGNAL_DEFS`(12신호), `model.weights`, `analysis.score`, `analysis.signalIds`, `analysis.conditionsMet`
* **상세 발동 조건 (Logic):**
  1. 일봉 데이터가 **55봉 미만**이면 매수 후보에서 제외됩니다.
  2. 아래 12개 신호 중 충족 개수가 **전체의 80% 이상**(12개 중 **최소 10개**)이면 `analysis.buy = true`가 됩니다.
     * 이동평균 정배열(`ma_align`), 골든크로스(`ma_golden`), 20봉 위(`ma20`), 50일선 위(`ma50`), 5·20 단기 정배열(`ma5_align`)
     * RSI 상승(`rsi`: 42~68 구간에서 전일 대비 상승)
     * 거래량 증가(`volume`: 20일 평균 대비 115% 초과), 거래량 급증(`volume_surge`: 150% 초과)
     * MACD 상승(`macd`), 60일 고가 근접(`high_60`: 60일 고점의 97% 이상)
     * 매물대 돌파(`vp_breakout`), 양봉(`bull_bar`)
  3. 각 신호는 모델별 **가중치**가 있으며, 충족 시 `score`에 가중 합산됩니다.
* **진입 비중 및 수량 산정:** 이 단계에서는 **주문하지 않습니다**. 추천 목록·텔레그램·실매매 트리거용 **픽(pick)** 만 생성합니다.

---

### 🔹 시나리오 B: 고득점 픽 — 실매매/시뮬 실행 트리거 (2차)

* **설명:** 스크리너 후보 중 **가중 점수가 모델 만점 대비 설정 비율을 초과**한 경우에만, 등록된 **시뮬·실매매 프로그램**으로 매수 로직이 연결됩니다. (텔레그램 알림과 동일한 점수 문턱을 사용합니다.)
* **핵심 지표/변수:** `pick.score`, `pick.techModelId`, `pick.techModelWeights`, `program.minScoreRatio`, `meetsTelegramNotifyScore()`
* **상세 발동 조건 (Logic):**
  1. **가중 점수**가 `maxScore × ratio` **초과**해야 합니다. (`ratio` 기본 **0.8**, 프로그램별 `minScoreRatio`로 **0.5~1.0** 조정 가능)
  2. `pick.techModelId`와 **동일한 `modelId`** 를 쓰는 프로그램만 후보가 됩니다.
  3. 스캔 결과가 다건(`picks`)일 때는 픽마다 위 조건을 만족할 때만 `notifyHighScorePick` → `onHighScorePickForLiveTrading`이 호출됩니다.
* **진입 비중 및 수량 산정:** 아래 시나리오 C·D에서 프로그램 설정 금액으로 산정합니다.

> **참고:** 스크리너 1차 통과(신호 10/12개)와 2차 실행(가중 점수 80% 초과)은 **서로 다른 기준**입니다. 신호는 많지만 가중 점수가 낮으면 **목록에는 올 수 있으나 자동 매수는 되지 않을 수** 있습니다.

---

### 🔹 시나리오 C: 시뮬레이션 자동 매수

* **설명:** «시뮬 자동 시작» 상태(`status: sim`)인 프로그램이 고득점 픽을 받으면 **가상 체결**을 포트폴리오에 기록합니다.
* **핵심 지표/변수:** `program.simAutoBuy`, `program.maxOpenPositions`, `program.orderAmountKrw` / `orderAmountUsd`, `resolveOrderAmountForMarket()`, `DEDUPE_MS`(6시간)
* **상세 발동 조건 (Logic):**
  1. 프로그램 상태가 **`sim`** 이고, `simAutoBuy !== false` (기본값 **자동 매수 켜짐**).
  2. 픽의 시장이 프로그램 `markets`(국내/미국/코인)에 **포함**되어야 합니다.
  3. `pickMeetsProgramThreshold(program, pick)` — 프로그램 `minScoreRatio` 기준 가중 점수 통과.
  4. 동일 프로그램·동일 심볼에 **6시간 이내** 매수 기록이 없어야 합니다 (`programId:symbol` 중복 방지).
  5. 현재 보유 종목 수가 `maxOpenPositions`(기본 **5**, 1~50) 미만이거나, **이미 같은 종목을 보유** 중이면 추가 매수 가능.
* **진입 비중 및 수량 산정:**
  * **국내:** `orderAmountKrw`(기본 최소 **1만 원**, 상한 5억 원) ÷ 현재가 → **정수 주** 단위.
  * **코인:** `orderAmountKrw` 또는 `orderAmountUsd×환율` → KRW 금액 ÷ 빗썸 KRW 시세 → 소수 **8자리** 수량.
  * **미국:** 시뮬 기록은 가능하나, 실주문 어댑터는 별도 제한(아래 실매매 참고).

---

### 🔹 시나리오 D: 빗썸 코인 실매매 자동 매수

* **설명:** «빗썸 실매매 시작»으로 `armed` + `armedMarkets.crypto` 가 켜진 프로그램이, 고득점 **코인** 픽에 대해 빗썸 API **시장가(KRW 금액)** 매수를 시도합니다.
* **핵심 지표/변수:** `BITHUMB_LIVE_ORDERS_ENABLED`, 사용자 API Key/Secret, `CRYPTO_MIN_ORDER_KRW`(1만 원), `ord_type: price`, `isProgramArmedForMarket(program, "crypto")`
* **상세 발동 조건 (Logic):**
  1. 시나리오 B·C와 동일한 **모델·점수·시장·중복(6시간)·보유 한도** 조건.
  2. 프로그램 `status === "armed"` 이고 **`armedMarkets.crypto === true`**.
  3. 사용자 빗썸 API가 **ready**(Key+Secret 저장).
  4. 심볼이 빗썸 KRW 마켓으로 변환 가능 (`BTC-USDT` → `KRW-BTC` 등).
  5. `BITHUMB_LIVE_ORDERS_ENABLED=1` 일 때만 **실제 주문**; 그렇지 않으면 주문 실패(연동 테스트 모드).
* **진입 비중 및 수량 산정:**
  * 프로그램 **코인 1회 매수 금액(원)** 을 `floor`한 KRW로 **시장가 매수** (`side: bid`, `ord_type: price`).
  * 최소 **10,000원** 미만이면 거부.
  * 체결 후 `pollBithumbOrderFill`로 체결가를 받아 포트폴리오에 기록.

---

### 🔹 시나리오 E: 토스 국내주식 실매매 자동 매수 (제한적)

* **설명:** 국내(`markets.kr`) 실매매 채널은 토스 Open API 어댑터에 연결되어 있으나, **실주문은 환경 변수로 명시적 활성화 전까지 시뮬레이션 스텁**으로 동작합니다.
* **핵심 지표/변수:** `TOSS_LIVE_ORDERS_ENABLED`, `TOSS_API_KEY`, `orderAmountKrw`
* **상세 발동 조건 (Logic):**
  1. 시나리오 B·C와 동일한 프로그램·점수·중복·보유 조건.
  2. `armedMarkets.kr === true`, 미국 단독 실매매는 **지원 안 함**.
  3. `TOSS_LIVE_ORDERS_ENABLED !== "1"` 이면 `simulated: true` 주문 ID만 반환하고 체결 기록.
* **진입 비중 및 수량 산정:** `orderAmountKrw` 기준(국내 최소 1만 원), 실주문 스펙 확정 시 REST `/v1/orders`로 전달 예정.

---

### 🔹 시나리오 F: 매수 시 목표가·손절가 자동 산정 (청산 준비)

* **설명:** `autoSellAtTarget !== false`(기본 **켜짐**)인 프로그램은 매수 직후 **일봉·ATR·일목·매물대·구조 분석**으로 목표가·손절가를 붙입니다. 이후 자동 매도 판단의 기준이 됩니다.
* **핵심 지표/변수:** `targetSellPrice`, `stopLossPrice`, `exitScenarioNote`, `signalIds`, `roundTripFeeRate`, `MIN_RR`(손익비 1.25)
* **상세 발동 조건 (Logic):**
  1. 매수 체결 기록 시 `resolveLiveTradeExitTargets()` 호출.
  2. 손절 후보: 일목 구름·POC·스윙 저점·ATR 배수·진입 신호별 조정(`volume_surge` 시 타이트 등).
  3. 목표 후보: 매물대 상단·스윙 고점·60일 고점·ATR 배수 등; **리스크 대비 보상이 1.25배 미만**이면 목표를 끌어올림.
  4. 순손절 **약 -1.2% ~ -15%**, 순익 목표 **약 +2% ~ +28%** 범위로 클램프(수수료 반영).
* **진입 비중 및 수량 산정:** 매수 수량과 무관; **청산 가격 레벨**만 설정.

---

### 🔹 시나리오 G: 수동·API 시뮬 매수 (사용자 요청)

* **설명:** UI/API를 통해 특정 프로그램·종목에 **수동 시뮬 매수**를 넣을 수 있습니다 (`recordLiveTradeSimBuyAsync`).
* **상세 발동 조건:** 프로그램 존재, 시장 허용, 시세 조회 가능; 위 자동 스크리너 경로와 무관.
* **진입 비중:** 지정 가격 또는 현재가, 프로그램 1회 매수 금액 규칙 동일.

---

## 3. 매도(Sell) 및 청산(Exit) 시나리오

자동 매도는 `autoSellAtTarget`이 켜진 **시뮬·실매매(active)** 프로그램의 **미청산 보유**에 대해 **약 45초 주기**(환경 변수 `STOCK_LIVE_TRADE_AUTO_SELL_MS`, 15~120초)로 평가합니다. 프로그램 `sellHorizon`에 따라 **단기(short)·중기(medium)·장기(long)** 전략이 달라집니다.

공통 선행 조건:
* 현재가 시세 조회 성공
* `autoSellAtTarget === true`
* (실매매 코인) 빗썸 **주문 가능 수량** 범위 내에서만 매도

---

### 🔸 시나리오 A: 고정 목표가·손절가 도달 (모든 호라이즌 공통)

* **설명:** 매수 시 저장된 **목표가·손절가**에 현재가가 닿으면 즉시 청산 신호.
* **핵심 지표/변수:** `targetSellPrice`, `stopLossPrice`, `currentPrice`
* **상세 발동 조건 (Logic):**
  1. `currentPrice >= targetSellPrice` → **목표가 도달** 익절.
  2. `currentPrice <= stopLossPrice` → **손절가 도달** 손절.
* **청산 방식:** 시뮬은 포트폴리오 기록; 빗썸 실매매는 **시장가 전량(가능 수량)** 매도.

---

### 🔸 시나리오 B: 단기(short) — 5분봉 모멘텀·기술 청산

* **설명:** 기본 호라이즌(`sellHorizon: short` 또는 실매매 armed 기본). **5분봉**으로 단기 트레이딩 종료.
* **핵심 지표/변수:** `RSI(14)`, `MACD`, `recentSwingLow`, `sessionHigh`, `netReturnPct`, `holdHours`, `roundTripFeeRate`
* **상세 발동 조건 (Logic):**
  1. 매수 후 **5분 미만**이면 기술 청산 없음(목표/손절만).
  2. **RSI 과매수 후 하락:** 5분 RSI가 68 이상에서 꺾이고, 순수익 **+0.2%** 초과.
  3. **MACD 하락 전환:** MACD가 양수 구간에서 하락 전환, 순수익 **-0.5%** 초과.
  4. **단기 지지 이탈:** 최근 12봉 스윙 저점 **0.2%** 하향 이탈, 순수익 **+1.5%** 미만.
  5. **익절 트레일링:** 매수 후 고점 대비 **1%** 이상 하락 + 순수익 **+1.5%** 이상.
  6. **거래량 동반 음봉 2연속** + 순수익 **+0.5%** 이상.
  7. **타임컷:** 보유 **36시간** 이상 + 순수익 **+0.8%** 미만.
  8. 차트 부족 시: 보유 **48시간** + 순수익 **+1%** 미만이면 정체 청산.
* **청산 방식:** 전량(빗썸은 주문가능 수량 한도 내).

---

### 🔸 시나리오 C: 중기(medium) — 1시간봉 스윙 청산

* **설명:** `sellHorizon: medium`. **1시간봉** 기준 스윙·추세 약화 시 청산.
* **핵심 지표/변수:** 1시간 `swingLow`, `MACD`, `RSI`, `holdHours`
* **상세 발동 조건 (Logic):**
  1. 매수 후 **6시간 미만**이면 기술 청산 제한(목표/손절만).
  2. **스윙 저점 이탈**(20봉 저점 0.5% 하향).
  3. **MACD 약세 전환:** MACD가 양→음, 순수익 **+0.5%** 초과.
  4. **RSI 추세 약화:** 5봉 전 58 이상 → 현재 42 이하, 순수익 플러스.
  5. **타임컷:** 보유 **10일(240h)** + 순수익 **+1.5%** 미만.
  6. 차트 부족 시 **14일** + 순수익 **+2%** 미만.
* **청산 방식:** 전량.

---

### 🔸 시나리오 D: 장기(long) — 일봉 가치·구조 청산

* **설명:** `sellHorizon: long`. **일봉** 기준 추세·가치 실현.
* **핵심 지표/변수:** `stopLossPrice`, `targetSellPrice`, `ma200`, `swingLow`, `holdDays`
* **상세 발동 조건 (Logic):**
  1. **손절가**는 단기와 동일하게 우선 적용.
  2. **가치 목표가** 도달 시 익절(순수익 표시 포함).
  3. **200EMA 하향 이탈** + 순수익 **+5%** 미만 → 추세 훼손 청산.
  4. **구조적 지지 붕괴:** 스윙 저점 **3%** 하향 이탈.
  5. **타임컷:** 보유 **90일** + 순수익 **+3%** 미만.
* **청산 방식:** 전량.

---

### 🔸 시나리오 E: 빗썸 실매매 시장가 매도 실행

* **설명:** 위 청산 신호가 나고 프로그램이 **armed + crypto** 일 때 빗썸 **시장가 매도**(`side: ask`, `ord_type: market`).
* **상세 발동 조건:**
  1. API ready, `BITHUMB_LIVE_ORDERS_ENABLED=1`.
  2. 앱 보유 수량 vs 거래소 **주문 가능 수량** — 부족하면 가능 수량만 매도.
* **청산 방식:** 전량(조정된 수량); 체결가로 포트폴리오 갱신.

---

### 🔸 시나리오 F: 국내(KR) 실매매 자동 매도 — 미지원

* **설명:** armed + `market === kr` 포지션에서 청산 신호가 나도 **자동 주문하지 않고 로그만 남깁니다** («KR 실매매 자동매도 미지원 — 수동 매도 필요»).
* **청산 방식:** 사용자 수동 처리 필요.

---

### 🔸 시나리오 G: 빗썸 수동 매도 감지 (동기화)

* **설명:** 앱에는 보유가 있는데 거래소 잔고가 거의 없으면, **사용자가 빗썸에서 직접 판 것**으로 간주하고 앱 포트폴리오에 매도를 기록합니다.
* **핵심 지표/변수:** `EXCHANGE_ZERO_RATIO`, `listBithumbDoneOrdersForMarket`, `findAskFillAfter`
* **상세 발동 조건:** 거래소 보유 < 앱 수량의 임계 비율; 체결 내역 또는 현재가로 매도가 추정.
* **청산 방식:** 기록상 전량 청산(실제 매도는 이미 사용자가 수행).

---

### 🔸 시나리오 H: 자동 매도 비활성 (`autoSellAtTarget: false`)

* **설명:** 프로그램에서 «목표가 자동 매도»를 끄면 매수 시 **목표/손절가를 null**로 두고, **주기적 자동 매도 루프도 해당 포지션을 평가하지 않습니다**.
* **청산 방식:** 수동·외부 거래소 처리에 의존.

---

## 4. 예외 처리 및 예방 로직 (Risk Management)

| 구분 | 내용 |
|------|------|
| **중복 매수 방지** | 동일 프로그램·심볼 **6시간** 내 재매수 차단 (`DEDUPE_MS`). 실매매는 키에 `live:` 접두. |
| **포지션 한도** | `maxOpenPositions`(기본 5). 한도 초과 시 **신규 종목** 매수 거부; **기보유 종목**은 추가 매수 가능. |
| **최소 주문 금액** | 국내·코인 **1만 원** 이상 (`KR_MIN_ORDER_KRW`, `CRYPTO_MIN_ORDER_KRW`). |
| **실주문 안전 스위치** | `BITHUMB_LIVE_ORDERS_ENABLED`, `TOSS_LIVE_ORDERS_ENABLED`가 `1`이 아니면 **실거래소 주문 없음**(스텁/실패). |
| **API·자격 증명** | 빗썸/토스 미연동 시 arm 거부; 주문 실패 시 `lastError` 기록, 포트폴리오 기록 실패 시 **고아 주문 로그**·dedup으로 재주문 방지. |
| **시장 제한** | 미국 실매매 미지원; 코인은 토스 실주문 불가; 프로그램 `markets` 밖 시장 무시. |
| **시세·차트 실패** | 시세 없으면 자동 매도 스킵; 일봉 부족 시 **보수적 기본 목표/손절** 또는 단순 타임컷. |
| **수수료 반영** | 순수익·목표/손절가는 **왕복 수수료율**(`roundTripFeeRate`, 거래소별 조회) 반영. |
| **자동 매도 끄기** | `STOCK_LIVE_TRADE_AUTO_SELL=0`이면 폴러 자체 미기동. |
| **수동 매도·잔고 불일치** | 빗썸 잔고 0 감지 시 체결 역추적 후 앱 기록 동기화. |
| **시뮬 피드백(학습 보조)** | `live-trade-sim-feedback.js`가 시뮬 결과로 `minScoreRatio` 상향·`autoSellAtTarget` 복구 등 **프로그램 파라미터 자동 조정** 제안(운영 정책에 따름). |

---

## 5. 아키텍트 의견 및 개선 제안

1. **이중 매수 문턱의 명확화**  
   스크리너는 «신호 80% 충족», 자동 매매는 «가중 점수 80% 초과»입니다. 기획·UI에서 **「추천 목록」과 「실제 주문」 조건이 다를 수 있음**을 사용자에게 노출하면 혼선이 줄어듭니다.

2. **국내 실매매 매도 공백**  
   KR 실매매 **매수 파이프라인은 있으나 자동 매도는 의도적으로 차단**되어 있습니다. 국내 실매매를 켤 계획이면 매도 어댑터·호가 정책을 매수와 대칭으로 설계해야 합니다.

3. **과적합·파라미터 다수**  
   12신호 + 일봉 목표 산정 + 5분/1시간/일봉 청산 + 신호별 가중 조정은 **백테스트 없이 규칙이 누적**된 형태입니다. 시장·종목군별 **walk-forward 검증**과 호라이즌별 성과 분리 리포트를 권장합니다.

4. **6시간 dedupe vs 스윙 전략**  
   같은 종목 재진입을 6시간 막지만, 중·장기 전략에서는 **추가 분할 매수/피라미딩 정책이 없습니다**. 의도된 보수성인지, 기회 비용을 검토할 가치가 있습니다.

5. **실주문 플래그 의존**  
   운영 실수로 `BITHUMB_LIVE_ORDERS_ENABLED` 미설정 시 «연동됨» UI와 실제 무주문 상태가 어긋날 수 있습니다. **프로그램 armed 시 실주문 가능 여부를 상태 API에 명시**하는 것이 안전합니다.

6. **고정 % 목표(`takeProfitPct`/`stopLossPct`)**  
   코드상 **신규 매수는 `live-trade-exit-scenario` 우선**이나, 프로그램 필드에 레거시 % 필드가 남아 있습니다. 문서·UI에서 «구버전 필드 무시»를 정리하면 유지보수에 유리합니다.

7. **보완 지표 제안**  
   * 매수: **시장 레짐 필터**(지수 추세·VIX·BTC 도미넌스 등)  
   * 매도: **슬리피지·호가 스프레드** 상한, **일일 최대 손실(Daily loss limit)**  
   * 공통: **스크리너·체결 지연** 모니터링(픽 시점 가격 vs 체결가 괴리)

---

*본 보고서는 저장 시점의 `c:/Stock/server` 소스 코드를 기준으로 작성되었습니다. 환경 변수·프로그램 설정·API 활성화 여부에 따라 실제 거래 행위는 달라질 수 있습니다.*
